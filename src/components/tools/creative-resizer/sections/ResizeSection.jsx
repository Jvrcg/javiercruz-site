export default function ResizeSection() {
  return (
    <div className="flex flex-col gap-2">
      <h2 className="text-lg font-semibold text-gray-900">Resize</h2>
      <p className="text-sm text-gray-500">Preset and custom output sizing lands in a later phase.</p>
    </div>
  );
}
